requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Core dependencies
pandas>=1.3.0

# Optional dependencies for enhanced functionality
numpy>=1.20.0
geopy>=2.2.0

# Development and logging
logging>=0.4.9.6

# For better HTTP handling and retries
urllib3>=1.26.0
certifi>=2021.5.25

# For concurrent processing (included in Python 3.2+ but listed for clarity)
# concurrent.futures - This is part of the standard library in Python 3.2+

# Optional: For more advanced geocoding features
googlemaps>=4.6.0
mapbox>=0.18.1

# For data validation and type hints (if using Python < 3.8)
typing-extensions>=3.10.0

# For better error handling and retries
tenacity>=8.0.0

# Optional: For configuration management
python-dotenv>=0.19.0

# For better JSON handling
ujson>=4.0.0

# Optional: For progress bars
tqdm>=4.62.0